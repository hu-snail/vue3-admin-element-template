/**
 * @description 主题配置
 * @author hu-snail
 */
export const themeConfig = {
  // 模式 horizontal / vertical
  mode: 'horizontal',
  // 主题
  theme: '#08a17e',
  // 是否固定头部
  fixedHead: true,
  // 是否显示全屏
  fullScreen: true,
  // 是否显示刷新
  refresh: true,
  // 是否显示通知
  notice: true,
  // 是否显示面包导航
  isBreadcrumb: true,
  // 是否显示logo
  isLogo: true,
  // 是否显示标签
  tag: true,
  // 菜单背景
  menuBgColor: '#ffffff',
  // 是否展开菜单
  collapse: false,
};
